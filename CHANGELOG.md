## v1.0.0
- Initial Home Assistant add-on release, ported from immich-tiktok-remover.
- Built on the stable-lite detection path (filename/creation-date heuristics only, ~100MB image); the ML-based watermark recognition path (easyocr/moviepy, stable ~4.1GB) is intentionally excluded.
